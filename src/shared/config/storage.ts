// 기기에 남는 저장소 키. 앱을 지우기 전까지 사용자 단말에 그대로 보존되므로
// 값 자체가 하나의 스키마다.
//
// 값을 바꾸면 앱은 이름이 바뀐 빈 칸을 읽고, 옛 키에 있던 기록은 단말에 남은
// 채 아무도 읽지 않는 고아가 된다. 스냅샷은 서버에서 다시 받아 복구되지만
// 큐는 서버에 사본이 없으므로 그대로 유실된다. 그래서 이름을 바꾸려면
// 배포 규모에 따라 둘 중 하나가 반드시 따라와야 한다.
// - 배포 전(테스터 소수): 사용자에게 동기화 완료 후 업데이트를 안내한다.
// - 배포 후: 옛 키를 읽어 새 키로 옮기는 이관 코드를 함께 넣는다.
//
// 접미사 `.v1`은 브랜드가 아니라 저장 형식의 버전이다. 형식이 호환되지 않게
// 바뀔 때만 올린다.

/** 서버로 아직 보내지 못한 변경. 서버에 사본이 없는 원본이다. */
export const OFFLINE_QUEUE_STORAGE_KEY = 'zaringovy.offline-mutations.v1';

/** 서버 데이터의 읽기 전용 캐시. 지워져도 다음 동기화로 복구된다. */
export const OFFLINE_SNAPSHOT_STORAGE_KEY = 'zaringovy.offline-snapshots.v1';
